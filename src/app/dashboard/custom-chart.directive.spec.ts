import { CustomChartDirective } from './custom-chart.directive';

describe('CustomChartDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomChartDirective();
    expect(directive).toBeTruthy();
  });
});
